ruby:
  currency_code = (@session&.currency.presence || "jpy").to_s.downcase
  amount_display = if @amount.present? && @amount.to_i > 0
    if currency_code == "jpy"
      "¥#{number_with_delimiter(@amount.to_i)}"
    else
      major = (@amount.to_f / 100.0)
      "$#{number_with_delimiter(format('%.2f', major))}"
    end
  end
  plan_label = if I18n.locale.to_s == "en" && @plan_type.present?
    (Subscription::PLAN_CATALOG.dig(@plan_type.to_sym, :name_en) || @plan_name)
  else
    @plan_name
  end

.drafify-lp.checkout-success-page
  .checkout-success
    .checkout-success__card
      .checkout-success__hero
        .checkout-success__icon-ring
          .checkout-success__icon
            svg viewBox="0 0 24 24" fill="none" aria-hidden="true"
              path d="M6 12.5l3.5 3.5L18 8" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"
        h1.checkout-success__title = t("drafity.auth.checkout_success_heading")
        p.checkout-success__lead
          = t("drafity.auth.checkout_success_thanks")
          br
          = t("drafity.auth.checkout_success_applied")

      .checkout-success__details
        .checkout-success__detail
          .checkout-success__detail-icon
            svg viewBox="0 0 24 24" fill="none" aria-hidden="true"
              path d="M4 6h16M4 12h16M4 18h10" stroke="currentColor" stroke-width="2" stroke-linecap="round"
          .checkout-success__detail-body
            span.checkout-success__detail-label = t("drafity.auth.checkout_selected_plan")
            strong.checkout-success__detail-value = plan_label

        - if amount_display.present?
          .checkout-success__detail
            .checkout-success__detail-icon
              svg viewBox="0 0 24 24" fill="none" aria-hidden="true"
                path d="M12 2v20M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
            .checkout-success__detail-body
              span.checkout-success__detail-label = t("drafity.auth.checkout_amount")
              strong.checkout-success__detail-value = amount_display

        - if @invoice_id.present?
          .checkout-success__detail.checkout-success__detail--wide
            .checkout-success__detail-icon
              svg viewBox="0 0 24 24" fill="none" aria-hidden="true"
                path d="M9 12h6m-6 4h6M7 4h7l5 5v11a2 2 0 01-2 2H7a2 2 0 01-2-2V6a2 2 0 012-2z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
            .checkout-success__detail-body
              span.checkout-success__detail-label = t("drafity.auth.checkout_success_payment_id")
              strong.checkout-success__detail-value.checkout-success__detail-value--mono = @invoice_id

      = link_to dashboard_root_path(client_id: current_client.id), class: "checkout-success__btn" do
        span = t("drafity.auth.checkout_success_to_dashboard")
        svg viewBox="0 0 24 24" fill="none" aria-hidden="true"
          path d="M5 12h14M13 6l6 6-6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"


= yahoo_ads_conversion_tags
