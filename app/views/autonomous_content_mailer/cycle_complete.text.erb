<%= @client.name.presence || @client.email %> 様

自律生成サイクルが完了しました。

親記事: <%= @pillar&.title %>
完了した子記事: <%= @run.completed_child_count %>件

<% if @next_titles.any? %>
次の親記事タイトル案:
<% @next_titles.each_with_index do |title, index| %>
<%= index + 1 %>. <%= title %>
<% end %>

新しいサイクルを開始する場合はこちら:
<%= @new_run_url %>
<% end %>

--
Drafity
