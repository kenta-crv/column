/ AIジャンル提案モーダル
#genre-draft-modal.modal-overlay style="display: none;"
  .modal-content
    .modal-header
      h3 = t("drafity.dashboard.genres.draft_title")
      button.modal-close type="button" onclick="closeGenreDraftModal()" ✕
    .modal-body
      p.genre-draft-modal__lead
        = t("drafity.dashboard.genres.draft_lead")
        - if sub_config[:allowed]
          |
          - if sub_config[:unlimited]
            = t("drafity.dashboard.genres.draft_sub_unlimited")
          - else
            = t("drafity.dashboard.genres.draft_sub_max", max: sub_config[:max])
        - else
          |
          = t("drafity.dashboard.genres.draft_sub_off")
      .form-group
        label for="genre-draft-service-name" = t("drafity.dashboard.genres.draft_service")
        input#genre-draft-service-name type="text" placeholder=t("drafity.dashboard.genres.draft_ph_service")
      .form-group
        label for="genre-draft-strong-points" = t("drafity.dashboard.genres.draft_strong")
        textarea#genre-draft-strong-points rows="4" placeholder=t("drafity.dashboard.genres.draft_ph_strong")
      #genre-draft-status.genre-quick-setup-status hidden=true
      button#genre-draft-generate-btn.btn.btn-primary type="button" style="width: 100%; margin-top: 8px; height: 38px; display: inline-flex; align-items: center; justify-content: center; gap: 8px; background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%); color: #ffffff; border: none; border-radius: 6px; font-size: 13.5px; font-weight: 600; cursor: pointer; box-shadow: 0 4px 12px rgba(124, 58, 237, 0.2); transition: opacity 0.2s;" onmouseover="this.style.opacity='0.9'" onmouseout="this.style.opacity='1'"
        svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"
          path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"
        span = t("drafity.dashboard.genres.draft_apply")
