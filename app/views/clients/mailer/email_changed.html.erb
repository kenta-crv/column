<p><%= t("drafity.mailer.hello", email: @email) %></p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t("drafity.mailer.email_changed.changing", email: @resource.unconfirmed_email) %></p>
<% else %>
  <p><%= t("drafity.mailer.email_changed.changed", email: @resource.email) %></p>
<% end %>
