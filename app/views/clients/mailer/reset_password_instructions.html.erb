<p><%= t("drafity.mailer.hello", email: @resource.email) %></p>

<p><%= t("drafity.mailer.reset_password.intro") %></p>

<p><%= link_to t("drafity.mailer.reset_password.action"), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t("drafity.mailer.reset_password.ignore") %></p>
<p><%= t("drafity.mailer.reset_password.unchanged") %></p>
