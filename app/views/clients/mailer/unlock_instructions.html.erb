<p><%= t("drafity.mailer.hello", email: @resource.email) %></p>

<p><%= t("drafity.mailer.unlock.intro") %></p>

<p><%= t("drafity.mailer.unlock.link_intro") %></p>

<p><%= link_to t("drafity.mailer.unlock.action"), unlock_url(@resource, unlock_token: @token) %></p>
