<p><%= t("drafity.mailer.welcome", email: @email) %></p>

<p><%= t("drafity.mailer.confirmation.intro") %></p>

<p><%= link_to t("drafity.mailer.confirmation.action"), confirmation_url(@resource, confirmation_token: @token) %></p>
