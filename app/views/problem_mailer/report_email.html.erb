<p>以下の内容で問題報告がありました。</p>

<table>
  <tr>
    <th>会社名</th>
    <td><%= @problem.company %></td>
  </tr>
  <tr>
    <th>メールアドレス</th>
    <td><%= @problem.email %></td>
  </tr>
  <tr>
    <th>内容</th>
    <td>
      <% if @problem.respond_to?(:message) && @problem.message.present? %>
        <%= simple_format(@problem.message) %>
      <% elsif @problem.respond_to?(:body) && @problem.body.present? %>
        <%= simple_format(@problem.body) %>
      <% else %>
        <span style="color: #999;">（内容なし）</span>
      <% end %>
    </td>
  </tr>
</table>

<% if @photo_attachment_name.present? && attachments[@photo_attachment_name].present? %>
  <p>添付された写真:</p>
  <p>
    <%= image_tag attachments[@photo_attachment_name].url, width: 400 %>
  </p>
<% elsif @problem.photo.present? %>
  <p>添付された写真:</p>
  <p>
    <%= link_to @problem.photo.url, @problem.photo.url %>
  </p>
<% end %>
